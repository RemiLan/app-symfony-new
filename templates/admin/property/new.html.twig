{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        <h1>Créer un nouveau bien</h1>
        {{ include('admin/property/_form.html.twig', {form: form, button: 'Créer'}) }}
        {{ form_start(coco) }}
        {{ form_errors(coco) }}
            <h2>{{ form_label(coco.title) }}</h2>
            {# {{ form_row(coco.title, {'attr': {'class': 'cocop'}}) }} #}
            {{ form_widget(coco.title, {'attr': {'class': 'cocop'}}) }}
            {{ form_rest(coco) }}
        {# {{ form_end(coco) }} #}
    </div>
{% endblock %}